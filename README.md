# ASCII Art Generator

This project uses Python to accept an image path and convert it to instant terminal art, using `▀` and `▄` to handle transparency to ensure proper display.

## How to Use

1. Clone the repository:

## Requirements

- Python 3.x (tested on 3.14, older versions may or may not work)
- For futher dependencies, run: `pip install -r requirements.txt`
