# welcome

v1.0.0

personally set up welcome script to print an aesthetic greeting on terminal startup

## Usage

To use this, set up a JSON file in the `config` directory with the following structure:

```json
{
    // if this is not provided, the username
    // defaults to the name of your home directory
    "username": "Your Name"  
}
```
